version: "3.5"

services:
  bullboard:
    container_name: bullboard
    build:
      dockerfile: ./Dockerfile
      context: .
    restart: always
    stop_grace_period: 30s
    ports:
      - 3000:3000
    environment:
      REDIS_HOST: host
      REDIS_PASSWORD: pass

